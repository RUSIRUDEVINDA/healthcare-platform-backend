apiVersion: v1
kind: ConfigMap
metadata:
  name: patient-service-config
  namespace: healthcare
data:
  APP_ENV: "production"
  PORT: "8002"
  DATABASE_URL: "postgres://postgres:$(POSTGRES_PASSWORD)@postgres-service.healthcare.svc.cluster.local:5432/patient_db?sslmode=disable"
  RABBITMQ_URL: "amqp://admin:$(RABBITMQ_PASSWORD)@rabbitmq-service.healthcare.svc.cluster.local:5672/"

