apiVersion: v1
kind: Service
metadata:
  name: telemedicine-service
  namespace: healthcare
  labels:
    app: telemedicine-service
spec:
  selector:
    app: telemedicine-service
  ports:
    - name: http
      protocol: TCP
      port: 8009
      targetPort: 8009
  type: ClusterIP

