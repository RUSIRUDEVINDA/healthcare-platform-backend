apiVersion: v1
kind: Service
metadata:
  name: file-storage-service
  namespace: healthcare
  labels:
    app: file-storage-service
spec:
  selector:
    app: file-storage-service
  ports:
    - name: http
      protocol: TCP
      port: 8010
      targetPort: 8010
  type: ClusterIP
